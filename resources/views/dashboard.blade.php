<x-app-layout title="Dashboard">
    <section class="section"><div class="container"><div class="section-head"><x-badge>{{ auth()->user()->program }}</x-badge><h1 class="section-title">Student <span class="accent-red">Dashboard</span></h1></div><div class="grid grid-3">@foreach($stats as $label => $value)<x-dashboard-stat :label="$label" :value="$value" />@endforeach</div><h2 style="margin-top:42px;">Recent Orders</h2>@if($orders->count())<x-table><table><thead><tr><th>Order</th><th>Status</th><th>Payment</th><th>Total</th><th></th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->order_code }}</td><td><x-status-pill :status="$order->status" /></td><td><x-status-pill :status="$order->payment_status" /></td><td>₱{{ number_format($order->total_amount, 2) }}</td><td><x-button href="{{ route('orders.show', $order) }}" variant="outline">View</x-button></td></tr>@endforeach</tbody></table></x-table>@else<x-empty-state title="No orders yet" message="Start from the merchandise catalog." />@endif</div></section>
</x-app-layout>
