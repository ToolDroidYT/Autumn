<x-app-layout title="Orders"><section class="section"><div class="container"><div class="section-head"><x-badge>Tracking</x-badge><h1 class="section-title">My <span class="accent-red">Orders</span></h1></div>@if($orders->count())<x-table><table><thead><tr><th>Order</th><th>Status</th><th>Payment</th><th>Total</th><th>Receipt</th><th></th></tr></thead><tbody>@foreach($orders as $order)<tr><td>{{ $order->order_code }}</td><td><x-status-pill :status="$order->status" /></td><td><x-status-pill :status="$order->payment_status" /></td><td>₱{{ number_format($order->total_amount, 2) }}</td><td>@if($order->receipt)<a style="color:var(--orange);" href="{{ route('receipts.show', $order) }}">Receipt</a>@else Pending @endif</td><td><x-button href="{{ route('orders.show', $order) }}" variant="outline">View</x-button></td></tr>@endforeach</tbody></table></x-table>{{ $orders->links() }}@else<x-empty-state title="No orders" message="No merchandise orders have been placed." />@endif</div></section></x-app-layout>
