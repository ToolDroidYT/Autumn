<x-app-layout :title="$order->order_code"><section class="section"><div class="container grid grid-2"><div><x-badge>Order</x-badge><h1 class="section-title" style="text-align:left;">{{ $order->order_code }}</h1><div class="actions" style="margin-bottom:20px;"><x-status-pill :status="$order->status" /><x-status-pill :status="$order->payment_status" /></div><x-table><table><thead><tr><th>Item</th><th>Size</th><th>Qty</th><th>Total</th></tr></thead><tbody>@foreach($order->items as $item)<tr><td>{{ $item->product->name }}</td><td>{{ $item->size }}</td><td>{{ $item->quantity }}</td><td>₱{{ number_format($item->line_total, 2) }}</td></tr>@endforeach</tbody></table></x-table><h2>Total: ₱{{ number_format($order->total_amount, 2) }}</h2>@if($order->receipt)<x-button href="{{ route('receipts.show', $order) }}" variant="outline">View Receipt</x-button>@endif</div><x-card><h2>Upload Payment Proof</h2><p>Required downpayment: ₱{{ number_format($order->downpayment_amount, 2) }}</p><form method="POST" action="{{ route('payments.upload', $order) }}" enctype="multipart/form-data">@csrf<x-input label="Amount" name="amount" type="number" step="0.01" value="{{ $order->downpayment_amount }}" /><x-select label="Method" name="method" :options="['GCash'=>'GCash','Maya'=>'Maya','Cash'=>'In-person Cash']" /><x-input label="Reference Number" name="reference_number" /><div class="field"><label for="proof">Proof Image</label><input id="proof" name="proof" type="file" accept="image/*" class="input" data-preview-input="#proof-preview"><img id="proof-preview" hidden style="margin-top:12px;border-radius:10px;max-height:180px;"></div><x-button type="submit">Submit Proof</x-button></form><h3>Payment History</h3>@foreach($order->payments as $payment)<p><x-status-pill :status="$payment->status" /> ₱{{ number_format($payment->amount, 2) }} · {{ $payment->method }}</p>@endforeach</x-card></div></section></x-app-layout>
