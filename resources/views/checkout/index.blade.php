<x-app-layout title="Checkout">
    <section class="section"><div class="container grid grid-2"><div><x-badge>Checkout</x-badge><h1 class="section-title" style="text-align:left;">Confirm <span class="accent-red">Order</span></h1><p class="section-copy" style="margin-inline:0;">A 50% downpayment is required before the order is confirmed. Orders can be edited or cancelled before payment confirmation only.</p><form method="POST" action="{{ route('checkout.place') }}">@csrf<x-textarea label="Notes" name="notes" placeholder="Size notes, pickup remarks, or payment context." /><x-button type="submit">Place Order</x-button></form></div><x-card><h2>Summary</h2>@foreach($cart['items'] as $item)<div class="meta-row" style="margin-bottom:12px;"><span>{{ $item['product']->name }} × {{ $item['quantity'] }}</span><strong>₱{{ number_format($item['line_total'], 2) }}</strong></div>@endforeach<hr style="border-color:var(--line);"><h2>Total: ₱{{ number_format($cart['total'], 2) }}</h2><p>Required downpayment: ₱{{ number_format($cart['total'] * .5, 2) }}</p></x-card></div></section>
</x-app-layout>
