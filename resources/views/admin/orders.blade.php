<x-app-layout title="Admin Orders"><section class="section"><div class="container admin-shell"><x-admin-sidebar /><div><div class="section-head" style="text-align:left;margin-bottom:28px;"><x-badge>Admin</x-badge><h1 class="section-title" style="text-align:left;">Order <span class="accent-red">Operations</span></h1></div>
<x-table><table><thead><tr><th>Order</th><th>Student</th><th>Status</th><th>Payment</th><th>Total</th><th>Action</th></tr></thead><tbody>@foreach($orders as $order)<tr><td><a style="color:var(--orange);" href="{{ route('orders.show', $order) }}">{{ $order->order_code }}</a></td><td>{{ $order->user->name }}<br><small>{{ $order->user->program }}</small></td><td><x-status-pill :status="$order->status" /></td><td><x-status-pill :status="$order->payment_status" /></td><td>₱{{ number_format($order->total_amount, 2) }}</td><td><form method="POST" action="{{ route('admin.orders.status', $order) }}" data-confirm="Update order status?">@csrf @method('PATCH')<select class="select" name="status"><option>pending</option><option>confirmed</option><option>in_production</option><option>ready_for_pickup</option><option>claimed</option><option>cancelled</option></select><button class="btn btn-outline">Save</button></form></td></tr>@endforeach</tbody></table></x-table>{{ $orders->links() }}
</div></div></section></x-app-layout>
