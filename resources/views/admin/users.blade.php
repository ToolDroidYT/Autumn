<x-app-layout title="Admin Users"><section class="section"><div class="container admin-shell"><x-admin-sidebar /><div><div class="section-head" style="text-align:left;margin-bottom:28px;"><x-badge>Admin</x-badge><h1 class="section-title" style="text-align:left;">User <span class="accent-red">Access</span></h1></div>
<x-table><table><thead><tr><th>User</th><th>Program</th><th>Role</th><th>Action</th></tr></thead><tbody>@foreach($users as $user)<tr><td>{{ $user->name }}<br><small>{{ $user->email }}</small></td><td>{{ $user->program }}</td><td>{{ $user->role->display_name }}</td><td><form method="POST" action="{{ route('admin.users.role', $user) }}" data-confirm="Update user role?">@csrf @method('PATCH')<select name="role_id" class="select">@foreach($roles as $role)<option value="{{ $role->id }}" @selected($user->role_id === $role->id)>{{ $role->display_name }}</option>@endforeach</select><button class="btn btn-outline">Save</button></form></td></tr>@endforeach</tbody></table></x-table>{{ $users->links() }}
</div></div></section></x-app-layout>
