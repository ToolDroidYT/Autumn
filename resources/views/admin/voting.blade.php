<x-app-layout title="Admin Voting"><section class="section"><div class="container admin-shell"><x-admin-sidebar /><div><div class="section-head" style="text-align:left;margin-bottom:28px;"><x-badge>Admin</x-badge><h1 class="section-title" style="text-align:left;">Design <span class="accent-red">Voting</span></h1></div>
<x-card style="margin-bottom:24px;"><form method="POST" action="{{ route('admin.voting.store') }}" class="grid grid-2">@csrf<x-input label="Title" name="title" /><x-select label="Program" name="program" :options="['DCE'=>'DCE','IT'=>'IT','CS'=>'CS','CSIT/DCE'=>'CSIT/DCE','CODES'=>'CODES']" /><x-input label="Closes At" name="closes_at" type="datetime-local" /><div style="grid-column:1/-1;"><x-textarea label="Description" name="description" /></div><x-button type="submit">Create Vote</x-button></form></x-card><div class="grid grid-2">@foreach($votes as $vote)<x-card><div class="meta-row"><h3>{{ $vote->title }}</h3><x-status-pill :status="$vote->status" /></div><p>{{ $vote->description }}</p>@foreach($vote->options as $option)<div class="meta-row"><span>{{ $option->label }}</span><strong>{{ $option->votes_count }} votes</strong></div>@endforeach<form method="POST" action="{{ route('admin.voting.options.store', $vote) }}" style="margin-top:14px;">@csrf<x-input label="New Option" name="label" /><x-button type="submit" variant="outline">Add Option</x-button></form></x-card>@endforeach</div>{{ $votes->links() }}
</div></div></section></x-app-layout>
